%help plz
% Игроки
player(ivan).
player(oleg).
player(artem).
player(vanya).

% Персонажи
char(a).
char(aba).
char(bebe).
char(baba).
% злодеи
char(buga).
char(vuga).

% Привязки персонажей к классам
playing(ivan, a).
playing(oleg, ada).
playing(artem, bebe).
playing(vanya, baba).

% Классы
class(rogue).
class(bard).
class(mage).
class(warrior).

%Рассы
race(human).
race(elf).
race(goblin).
race(dvarf).

% Места
village(ada).
village(gaga).

% Инфа о рождении: год рождения, город
birth_info(a, 1899, ada).
birth_info(ada, 1828, ada).
bitth_info(bebe, 1000, gaga).
bitth_info(baba, 1830, ada).

% Принадлежность к рассе
born_race(a, human).
born_race(aba, goblin).
born_race(bebe, elf).
born_race(baba, dvarf).

% Разделение по классам
char_info(a, bard).
char_info(ada, rouge).
char_info(bebe, mage).
char_info(baba, warrior).


% Правила

%


























